<div>
  <h5>comment from <%= comment.foodie.name %> <%= distance_of_time_in_words(Time.now, comment.created_at) %> ago</h5>
</div>
<div>
  <p><%= comment.text %></p>
</div>
<%= link_to "delete comment", "/posts/#{@post.id}/comments/#{comment.id}", method: :delete %>